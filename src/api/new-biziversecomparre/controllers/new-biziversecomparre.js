'use strict';

/**
 * new-biziversecomparre controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::new-biziversecomparre.new-biziversecomparre');
